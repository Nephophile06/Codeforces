#include <bits/stdc++.h>
using namespace std;

int main() 
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--) 
    {
        string expr;
        cin >> expr;
        cout << (expr[0] - '0') + (expr[2] - '0') << "\n";
    }

    return 0;
}
