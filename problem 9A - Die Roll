#include <iostream>
using namespace std;
int main() 
{
    int y, w;
    cin >> y >> w;
    string arr[] = {"1/6", "1/3", "1/2", "2/3", "5/6", "1/1"};

    if (y == 0 || w == 0)
        cout << "0/1" << endl;
    
    int x = max (y, w);
    cout << arr[6-x] << endl;

    return 0;
}
