#include <iostream>
using namespace std;

int main() 
{
    int t;
    cin >> t;  
    while (t--) 
    {
        int n;
        cin >> n; 
        long long total_sum = (long long)n * (n + 1) / 2;
        cout << total_sum / n << endl;
    }
    return 0;
}
